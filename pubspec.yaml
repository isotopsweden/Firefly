name: firefly
description: A lightweight Flutter Widget for interacting with your Firestore database.
version: 0.1.0
author: Isotop
homepage: https://www.isotop.se/
issue_tracker: https://github.com/isotopsweden/Firefly/issues
repository: https://github.com/isotopsweden/Firefly

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^4.3.2+2
  cloud_firestore: ^0.14.2

dev_dependencies:
  flutter_test:
    sdk: flutter